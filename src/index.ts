// Load environment variables first
import './env';

// Re-export mastra instance
export { mastra } from './mastra/index';

// This is the main entry point for the application
console.log('4keys-mastra application initialized');